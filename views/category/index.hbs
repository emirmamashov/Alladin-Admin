    <a href="/category/create" class="btn btn-primary">Добавить категории</a>
    <div class="row">
        <table class="table table-striped table-hover ">
            <thead>
            <tr>
                <th>Наименование</th>
                <th></th>
            </tr>
            </thead>
            <tbody>
            {{# each categories}}
                <tr>
                    <td>{{this.name}}</td>
                    <td>
                        <a href="/category/delete/{{this._id}}" class="btn btn-success pull-right" role="button">Remove</a>
                        <a href="/category/edit/{{this._id}}" class="btn btn-success pull-right" role="button">Edit</a>
                    </td>
                </tr>
            {{/each}}
            </tbody>
        </table>
    </div>